# Core ML + numerics
numpy>=1.23
pandas>=1.5
scipy>=1.9
scikit-learn>=1.2
lightgbm>=4.0

# Useful utilities
tqdm>=4.65
joblib>=1.3

# Optional: read bigWig directly in Python (most pipelines use UCSC tools instead)
pybigwig>=0.3.22

# Optional: BED/GFF helpers (only if your scripts use them)
biopython>=1.81
pybedtools>=0.9.0
